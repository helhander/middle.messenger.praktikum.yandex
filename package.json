{
  "name": "sprint1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run clear && parcel build ./static/images/* --out-dir dist/images  --no-cache && parcel ./static/chats.pug --port 3000 --no-cache",
    "dev": "parcel build ./static/* && node ./server.js && parcel build ./static/*  --no-cache",
    "clear": "rm -rf ./dist",
    "build": "parcel build ./static/* && node ./server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.32.8"
  },
  "dependencies": {
    "express": "^4.17.1",
    "form.css": "^1.1.0",
    "fs": "^0.0.1-security",
    "pug": "^3.0.2"
  }
}
